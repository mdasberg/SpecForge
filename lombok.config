# Lombok stops here rather than reading configuration from parent directories, so a checkout's
# behaviour does not depend on where it happens to sit on disk.
config.stopBubbling = true

# Marks generated members so coverage and static analysis skip them instead of reporting
# constructors nobody wrote as untested.
lombok.addLombokGeneratedAnnotation = true
