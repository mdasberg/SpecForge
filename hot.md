## Work Log

## Next


- Start with `/opsx:apply add-project-skeleton` (issue #1). Stack is decided: Java 25 · Spring
  Boot 4 · Spring Modulith 2 · MySQL + Liquibase · Keycloak · React 19 + TS + Vite,
  package base `com.specforge`.
- Nothing is committed yet: `openspec/`, `.claude/` (5 opsx skills + 5 commands) and `.idea/` are
  untracked. Commit before the issue bodies' file references resolve on GitHub.
- Rebuild + republish recipe for the design canvas: `npm --prefix design run dev` (build + check +
  seed + serve), then republish `design/specforge.html` with `contract: "0.1.31"`, favicon 🔨, no
  `capabilities`.
- Prototype open questions, unchanged: whether the inert spec rows should each get their own review
  screen, and whether the 10–10.5px label scale should be raised across the design system.
