package com.specforge.repository;

/** What a review has to say about a proposed change, as far as the source forge is concerned. */
public enum ReviewOutcome {
    PENDING,
    APPROVED,
    CHANGES_REQUESTED
}
