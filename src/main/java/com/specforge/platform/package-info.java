/**
 * API conventions, error rendering, pagination, authentication and the mirrored identity. Shared by every capability module.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Platform")
package com.specforge.platform;
