/**
 * The API conventions every capability's controllers build on: the problem+json rendering, the
 * pagination envelope and its parameter parser.
 */
@org.springframework.modulith.NamedInterface("api")
package com.specforge.platform.api;
