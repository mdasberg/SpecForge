description: >
  The request conflicts with the current state of the resource: a duplicate connection to the
  same repository, branch and path, an illegal lifecycle transition, or any attempt to edit
  specification content.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/Problem.yaml
