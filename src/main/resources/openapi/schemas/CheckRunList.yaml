type: object
description: The checks tab for one review — every run against the current head, with the same summary the approval panel shows.
required: [items, summary]
properties:
  items:
    type: array
    items:
      $ref: ./CheckRun.yaml
  summary:
    $ref: ./ChecksSummary.yaml
