post:
  operationId: reopenThread
  tags: [ Discussions ]
  summary: Reopen a resolved thread, recording the caller and the current time.
  parameters:
    - name: threadId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: The reopened thread.
      content:
        application/json:
          schema:
            $ref: ../../schemas/Thread.yaml
    '401':
      $ref: ../../responses/Unauthorized.yaml
    '404':
      $ref: ../../responses/NotFound.yaml
    '409':
      $ref: ../../responses/Conflict.yaml
