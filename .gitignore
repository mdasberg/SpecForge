# Gradle / Java
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Node / frontend
node_modules/
frontend/dist/

# Agent tooling — local permission grants only, not project configuration
.claude/settings.local.json

# IDE
.idea/
*.iml
.vscode/

# Session working state — per-session scratch, not project history
hot.md
hot.prev.md

# Local environment
.env

# IntelliJ HTTP client — the public env file holds the composed dev realm's credentials, which are
# already in keycloak/realm-export.json. Anything pointing at a real environment goes here instead.
http/http-client.private.env.json
http/.idea/
